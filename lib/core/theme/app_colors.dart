import 'package:flutter/material.dart';

// Paleta de colores principal basada en el diseño
class AppColors {
  // Fondo principal (Lemon Chiffon)
  static const Color background = Color(0xFFFEFACD);
  
  // Botones CTA, valores clave (Ultra Violet)
  static const Color primary = Color(0xFF5F4A8B);
  
  // Gradientes y acentos
  static const Color primaryLight = Color(0xFF7E6AA8);
  
  // Superficies Glassmorphism
  static final Color glassSurface = Colors.white.withValues(alpha: 0.35);
  static final Color glassBorder = Colors.white.withValues(alpha: 0.60);
  
  // Textos
  static const Color textPrimary = Color(0xFF333333);
  static const Color textSecondary = Color(0xFF666666);
  static const Color textLight = Color(0xFF999999);
}
